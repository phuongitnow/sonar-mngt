# ============================================
# SonarQube Configuration (BẮT BUỘC)
# ============================================
# URL của SonarQube server
# Ví dụ: http://sonarqube:9000 hoặc http://192.168.1.100:9000
SONARQUBE_URL=http://sonarqube:9000

# Personal Access Token từ SonarQube (khuyến nghị)
# Tạo token tại: SonarQube UI → My Account → Security → Generate Token
# Ví dụ: squ_abc123xyz456def789ghi012jkl345mno678pqr901stu234vwx567yza890
SONARQUBE_TOKEN=your_sonarqube_token_here

# Username và Password (chỉ dùng nếu không có Token)
# Không khuyến nghị - nên dùng Token thay vì username/password
SONARQUBE_USERNAME=admin
SONARQUBE_PASSWORD=admin

# ============================================
# Email Configuration (BẮT BUỘC nếu muốn nhận notification)
# ============================================
# SMTP server host
# Gmail: smtp.gmail.com
# Outlook: smtp-mail.outlook.com
# Yahoo: smtp.mail.yahoo.com
MAIL_HOST=smtp.gmail.com

# SMTP server port
# Gmail/Outlook: 587 (TLS) hoặc 465 (SSL)
MAIL_PORT=587

# Email để gửi thông báo
MAIL_USERNAME=your_email@gmail.com

# App Password cho email (KHÔNG dùng password thông thường!)
# Với Gmail: Google Account → Security → App passwords → Generate
# App Password có 16 ký tự, viết liền không có khoảng trắng
MAIL_PASSWORD=your_app_password

# Email nhận thông báo kết quả cleanup
NOTIFICATION_EMAIL=admin@example.com

# ============================================
# Cleanup Configuration (TÙY CHỌN)
# ============================================
# Số ngày không scan thì project sẽ bị xóa (mặc định: 14 ngày)
DAYS_THRESHOLD=14

# Lịch chạy cleanup tự động (Cron expression)
# Format: second minute hour day month day-of-week
# Mặc định: 0 0 2 * * * (2 giờ sáng mỗi ngày)
# Ví dụ: 0 0 3 * * * (3 giờ sáng mỗi ngày)
CLEANUP_CRON=0 0 2 * * *

# ============================================
# Database Configuration (TÙY CHỌN / EXTERNAL DB)
# ============================================
# Nếu dùng DB bên ngoài, set SPRING_DATASOURCE_URL theo dạng:
# jdbc:postgresql://<host>:<port>/<database>
# Ví dụ: jdbc:postgresql://10.6.145.113:5434/dso
SPRING_DATASOURCE_URL=

# Username cho PostgreSQL (mặc định: admin)
DB_USERNAME=admin

# Password cho PostgreSQL (mặc định: admin123)
# Lưu ý: Nên đổi password mạnh hơn trong production!
DB_PASSWORD=admin123

